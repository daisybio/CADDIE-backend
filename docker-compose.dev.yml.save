version: '3.3'33333sdsdsdsdssds

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.DEV
    # Mount the current directory into `/code` inside the running container.
    volumes:
      - .:/code
